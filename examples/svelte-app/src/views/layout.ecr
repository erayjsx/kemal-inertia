<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <title>Kemal + Inertia + Svelte</title>
    <!-- Vite Assets -->
    <%= vite_tags("src/js/app.js") %>
  </head>
  <body>
    <div id="app" data-page="<%= HTML.escape(page) %>"></div>
  </body>
</html>
